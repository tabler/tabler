{
  "name": "preview",
  "private": true,
  "scripts": {
    "dev": "bundle exec jekyll serve --watch --livereload --drafts --config _config.yml --trace",
    "build": "bundle exec jekyll build --config _config.yml,_config.prod.yml"
  },
  "dependencies": {
    "@tabler/core": "workspace:*"
  }
}