{
	"name": "@tabler/preview-astro",
	"private": true,
	"type": "module",
	"version": "0.0.1",
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"assets": "node .build/copy-assets.mjs",
		"dev": "pnpm run assets && astro dev",
		"build": "pnpm run assets && astro build",
		"preview": "astro preview",
		"clean": "shx rm -rf dist public/dist public/preview public/css .astro",
		"test": "pnpm run build && node ../.build/parity.mjs"
	},
	"dependencies": {
		"@tabler/core": "workspace:*",
		"astro": "^7.0.7"
	},
	"devDependencies": {
		"@astrojs/mdx": "^7.0.2",
		"js-beautify": "^2.0.3",
		"prettier": "^3.9.5",
		"shiki": "^4.3.1",
		"shx": "^0.4.0"
	}
}
