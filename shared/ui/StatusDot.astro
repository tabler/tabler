---
interface Props {
	color?: string;
	animated?: boolean;
}

const { color, animated } = Astro.props;

const classes = [
	'status-dot',
	color && `status-${color}`,
	animated && 'status-dot-animated',
]
---

<span class:list={classes}></span>
