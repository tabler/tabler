# Custom properties the css references but never defines. Each one makes the
# declaration that reads it invalid, so the browser drops it - see
# check-css-vars.ts. The check fails on any name that is not listed here, and
# also on a listed name that no longer dangles, so fixing one means deleting
# its line.
#
# The list is empty: every var() in the css resolves. An entry added here is a
# bug waiting to be fixed, not an accepted exception.
