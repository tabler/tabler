---
"@tabler/core": minor
"@tabler/docs": patch
"@tabler/preview": patch
---

Added the `.text-shimmer` class, a highlight that sweeps across a label such as "Thinking…" while a task runs, with an optional `.text-shimmer-loader` spinner before the label.
