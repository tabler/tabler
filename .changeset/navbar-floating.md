---
"@tabler/core": minor
"@tabler/preview": minor
"@tabler/docs": patch
---

Added `.navbar-floating` and the `data-bs-navbar-style="floating"` theme setting for a navbar and sidebar with a `0.5rem` gap.
