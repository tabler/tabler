---
"@tabler/core": minor
"@tabler/preview": patch
"@tabler/docs": patch
---

Added a `--tblr-chart-1` … `--tblr-chart-5` palette and wired ApexCharts theme tokens, so every chart follows the color mode.
