---
"@tabler/core": minor
"@tabler/preview": minor
"@tabler/docs": patch
---

Added `Datepicker` plugin built on Vanilla Calendar Pro with a `datepicker` preview page, and removed Litepicker from `dist/libs`.
